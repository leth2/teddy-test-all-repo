# 스펙 라이프사이클 설정
SPEC_ID=spec-001
PORT=3100
UI_PORT=5100
BRANCH=feature/spec-001
WORKTREE_PATH=.worktrees/spec-001
ANTHROPIC_API_KEY=sk-ant-oat01-xxxx  # ~/.bashrc의 TEDDY_CLAUDE_CODE_TOKEN 값
CLINE_DIR=~/.cline                   # cline auth 인증 디렉토리 (Pi 2에서 cline auth -p anthropic -k $TOKEN 실행 후)
