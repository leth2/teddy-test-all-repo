# 스펙 라이프사이클 설정
SPEC_ID=spec-001
PORT=3100
UI_PORT=5100
BRANCH=feature/spec-001
WORKTREE_PATH=.worktrees/spec-001
ANTHROPIC_API_KEY=sk-ant-oat01-xxxx  # ~/.bashrc의 TEDDY_CLAUDE_CODE_TOKEN 값
CLINE_DIR=/home/leth2/.cline         # 절대경로 필수 (~는 docker compose에서 확장 안 됨)
                                     # cline auth -p anthropic -k $TOKEN 실행 후 생성됨
                                     # :ro 금지 — cline이 내부 파일 쓰기 필요
