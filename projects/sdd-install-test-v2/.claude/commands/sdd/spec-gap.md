---
description: 프로젝트 전체 갭분석. 로드맵 vs 스펙 vs 구현의 빈틈을 한꺼번에 탐지. 큰 그림에서 무엇이 빠졌는지 파악.
allowed-tools: Bash, Read, Write, Edit, Glob, Grep, LS
argument-hint: [--layer roadmap|coverage|tests|deps]
---

Read `.agents/skills/sdd-gap/SKILL.md` and follow its instructions.

Arguments: `$ARGUMENTS`

If `--layer`: run only the specified layer of gap analysis.
If no argument: run all 4 layers and produce full report.
